---
description: Create claude.md file with project instructions and schema
---

## Instructions

1. Create a file named `claude.md` in the `.claude` directory of the project
2. If the `.claude` directory does not exist, create it
3. Run the `init` command on the created directory
4. The `claude.md` file should be a maximum of 80 lines
5. Create a schema at the beginning of the file
